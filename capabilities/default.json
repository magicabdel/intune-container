{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Capabilities for the main Intune Container window.",
  "windows": ["main"],
  "permissions": ["core:default", "dialog:default"]
}
